version: '3.8'

services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana-provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana-provisioning/datasources:/etc/grafana/provisioning/datasources
    environment:
      - GF_INSTALL_PLUGINS=yesoreyeram-infinity-datasource # Replace with your desired plugin ID
      - GF_SECURITY_ADMIN_USER=myadminpasswd # Replace with your desired username
      - GF_SECURITY_ADMIN_PASSWORD=admin # Replace with your desired password
    restart: unless-stopped

volumes:
  grafana-data: